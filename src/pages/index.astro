---
import Welcome from '../components/Welcome.astro';
import Layout from '../layouts/Layout.astro';
import CanvasSvelte from '$components/svelte/CanvasSvelte.svelte';
import CanvasReact from '$components/react/CanvasReact.svelte';

// Astro에 오신 것을 환영합니다! 다음에 무엇을 해야 할지 궁금하신가요? Astro 문서를 확인하세요: https://docs.astro.build
// 이 코드를 사용하고 싶지 않으신가요? 이 파일의 모든 내용과 `assets`, `components`, `layouts` 디렉토리를 삭제하고 새로 시작하세요.
---

<Layout>
	<!-- <CanvasSvelte client:only="svelte" /> -->
	<CanvasReact client:only="react" />

</Layout>
